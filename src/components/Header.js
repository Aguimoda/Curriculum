import React from "react";

const Header = () => {
  return (
    <header>
      {/* Agrega tu contenido de encabezado aquí, como un logotipo o título */}
    </header>
  );
};

export default Header;
